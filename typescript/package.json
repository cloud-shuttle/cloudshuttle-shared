{
  "name": "cloudshuttle-shared",
  "version": "0.1.0",
  "description": "Shared TypeScript libraries for CloudShuttle services",
  "private": true,
  "workspaces": [
    "packages/components",
    "packages/hooks",
    "packages/types",
    "packages/utils",
    "packages/api",
    "packages/stores"
  ],
  "scripts": {
    "build": "pnpm run --recursive build",
    "test": "pnpm run --recursive test",
    "lint": "pnpm run --recursive lint",
    "clean": "pnpm run --recursive clean"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.0.0",
    "pnpm": "^8.0.0"
  },
  "engines": {
    "node": ">=18.0.0",
    "pnpm": ">=8.0.0"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/cloudshuttle/cloudshuttle-shared.git"
  },
  "license": "MIT",
  "author": "CloudShuttle Team"
}


